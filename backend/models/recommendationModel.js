// Recommendation.js

class Recommendation {
    constructor(destination, budget, accommodation, activities) {
        this.destination = destination;
        this.budget = budget;
        this.accommodation = accommodation;
        this.activities = activities;
    }
}

export default Recommendation;
